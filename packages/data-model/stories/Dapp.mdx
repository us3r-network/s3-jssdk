import { Meta, Source } from "@storybook/blocks";
import stringify from "json-stringify-pretty-compact";
import MainnetDefinition from "!!raw-loader!../src/dapp/dapp-runtime-composite-mainnet.js";
import TestnetDefinition from "!!raw-loader!../src/dapp/dapp-runtime-composite-testnet.js";

<Meta title="Dapp" />

# Dapp Model

## Mainnet

<Source
  code={stringify(JSON.parse(MainnetDefinition.split("=").pop()))}
  language="javascript"
/>

## Testnet

<Source
  code={stringify(JSON.parse(TestnetDefinition.split("=").pop()))}
  language="javascript"
/>
