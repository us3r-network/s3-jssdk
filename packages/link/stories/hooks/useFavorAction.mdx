import { Meta, ArgTypes, Source } from "@storybook/blocks";
import { UseFavorActionArgs, UseFavorActionReturns } from "./HooksTable";
import ExampleCode from "!!raw-loader!./examples/UseFavorAction.tsx";

<Meta title="React Hooks/useFavorAction" />

# useFavorAction

A React hook that returns the state of the favor action and a function to favor/unfavor.

## Args

<ArgTypes of={UseFavorActionArgs} />

## Returns

<ArgTypes of={UseFavorActionReturns} />

## Example

<Source code={ExampleCode} />
