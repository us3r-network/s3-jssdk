import { Meta, ArgTypes, Source } from "@storybook/blocks";
import { UseLinkVotesArgs, UseLinkVotesReturns } from "./TypeTable";
import ExampleCode from "!!raw-loader!./examples/UseLinkVotes.tsx";

<Meta title="React Hooks/useLinkVotes" />

# useLinkVotes

Fetches votes of a link.

## Args

<ArgTypes of={UseLinkVotesArgs} />

## Returns

<ArgTypes of={UseLinkVotesReturns} />

## Example

<Source code={ExampleCode} />
