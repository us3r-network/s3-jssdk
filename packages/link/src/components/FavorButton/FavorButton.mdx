import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as FavorButtonStories from "./FavorButton.stories";
import { RenderProps } from "./stories/Table";
import StyleCode from "!!raw-loader!../../ui/FavorButton/FavorButton.module.css";

<Meta of={FavorButtonStories} />

# FavorButton

FavorButton is an interactive component used for adding favorites.

## Example

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

FavorButton's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />
