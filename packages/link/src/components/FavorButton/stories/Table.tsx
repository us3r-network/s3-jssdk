import { ButtonRenderProps } from "react-aria-components";
import { FavorButtonRenderProps } from "../FavorButton";

export const RenderProps = (
  props: Partial<ButtonRenderProps & FavorButtonRenderProps>
) => {
  return null;
};
