import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as ScoreFormStories from "./ScoreForm.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/Score/form/ScoreForm.module.css";

<Meta of={ScoreFormStories} />

# ScoreForm

ScoreForm 是一个用于评分的表单组件。它为用户提供了一种简单的方式来对特定内容进行评分。

该组件适用于各种需要用户参与评分的场景，例如产品评价、文章评分等。

```jsx
import { ScoreForm } from "@us3r-network/link";
```

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

ScoreForm's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />

## Subcomponents

ScoreForm is a Context.Provider component that includes the following child components that can be used to render different parts of a form:

<ArgTypes of={Subcomponents} />
