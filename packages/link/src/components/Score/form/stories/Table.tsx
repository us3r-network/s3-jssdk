import { ScoreFormContextValue } from "../ScoreFormContext";
import ScoreFormElements from "../ScoreFormElements";

export const RenderProps = (props: Partial<ScoreFormContextValue>) => {
  return null;
};

export const Subcomponents = (props: Partial<typeof ScoreFormElements>) => {};
