import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as ScoreDashboardStories from "./ScoreDashboard.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/Score/dashboard/ScoreDashboard.module.css";

<Meta of={ScoreDashboardStories} />

# ScoreDashboard

ScoreDashboard shows scores in a simple and easy-to-understand way. It lets you see and summarize your scores in a visual way.

## Example

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

ScoreDashboard's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />

## Subcomponents

ScoreDashboard is a Context.Provider component that includes the following child components that can be used to render different parts of a form:

<ArgTypes of={Subcomponents} />
