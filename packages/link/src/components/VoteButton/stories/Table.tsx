import { ButtonRenderProps } from "react-aria-components";
import { VoteButtonRenderProps } from "../VoteButton";

export const RenderProps = (
  props: Partial<ButtonRenderProps & VoteButtonRenderProps>
) => {
  return null;
};
