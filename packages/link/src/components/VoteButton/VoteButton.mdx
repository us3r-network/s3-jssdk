import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as VoteButtonStories from "./VoteButton.stories";
import { RenderProps } from "./stories/Table";
import StyleCode from "!!raw-loader!../../ui/VoteButton/VoteButton.module.css";

<Meta of={VoteButtonStories} />

# VoteButton

The VoteButton is an interactive component used for expressing their love and support for the content.

## Example

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

VoteButton's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />
