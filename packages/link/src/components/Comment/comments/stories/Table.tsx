import { CommentsContextValue } from "../CommentsContext";
import CommentsElements from "../CommentsElements";

export const RenderProps = (props: Partial<CommentsContextValue>) => {};

export const Subcomponents = (props: Partial<typeof CommentsElements>) => {};
