import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as CommentAddFormStories from "./CommentAddForm.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/Comment/add-form/CommentAddForm.module.css";

<Meta of={CommentAddFormStories} />

# CommentAddForm

CommentAddForm is an interactive component used for adding comments. It provides users with a form to edit and submit comments.

## Example
<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

CommentAddForm's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />

## Subcomponents

CommentAddForm is a Context.Provider component that includes the following child components that can be used to render different parts of a form:

<ArgTypes of={Subcomponents} />
