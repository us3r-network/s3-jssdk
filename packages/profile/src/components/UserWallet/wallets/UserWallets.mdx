import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserWalletsStories from "./UserWallets.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/UserWallet/wallets/UserWallets.module.css";

<Meta of={UserWalletsStories} />

# UserWallets

UserWallets is used to display wallet information that a user owns. It can display wallet information for the currently logged-in user as well as for other users.

## Example
<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserWallets's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />

## Subcomponents

UserWallets is a Context.Provider component that includes the following child components that can be used to render different parts of a form:

<ArgTypes of={Subcomponents} />
