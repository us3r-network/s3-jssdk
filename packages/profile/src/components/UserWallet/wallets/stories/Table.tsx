import { UserWalletsContextValue } from "../UserWalletsContext";
import UserWalletsElements from "../UserWalletsElements";

export const RenderProps = (props: Partial<UserWalletsContextValue>) => {
  return null;
};

export const Subcomponents = (props: Partial<typeof UserWalletsElements>) => {};
