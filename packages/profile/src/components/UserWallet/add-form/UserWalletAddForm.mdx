import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserWalletAddFormStories from "./UserWalletAddForm.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/UserWallet/add-form/UserWalletAddForm.module.css";

<Meta of={UserWalletAddFormStories} />

# UserWalletAddForm

UserWalletAddForm 用于添加用户钱包信息，只有在用户登录的情况下才会可用。

```jsx
import { UserWalletAddForm } from "@us3r-network/profile";
```

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserWalletAddForm 的 children 可以是一个函数，它接收一个对象作为参数，该对象包含以下属性：

<ArgTypes of={RenderProps} />

## Subcomponents

UserWalletAddForm 是一个 Context.Provider 组件，它有一些子组件可以用来渲染表单的不同部分。

<ArgTypes of={Subcomponents} />
