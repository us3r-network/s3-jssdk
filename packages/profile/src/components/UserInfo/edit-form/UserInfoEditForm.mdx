import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserInfoEditFormStories from "./UserInfoEditForm.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/UserInfo/edit-form/UserInfoEditForm.module.css";

<Meta of={UserInfoEditFormStories} />

# UserInfoEditForm

UserInfoEditForm 用于编辑用户的基本信息，只有在用户登录的情况下才会是可用状态。

```jsx
import { UserInfoEditForm } from "@us3r-network/profile";
```

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserInfoEditForm 的 children 可以是一个函数，它接收一个对象作为参数，该对象包含以下属性：

<ArgTypes of={RenderProps} />

## Subcomponents

UserInfoEditForm 是一个 Context.Provider 组件，它有一些子组件可以用来渲染表单的不同部分。

<ArgTypes of={Subcomponents} />
