import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserAvatarStories from "./UserAvatar.stories";
import { RenderProps } from "./stories/Table";
import StyleCode from "!!raw-loader!../../ui/UserAvatar/UserAvatar.module.css";

<Meta of={UserAvatarStories} />

# UserAvatar

UserAvatar is a component used to display user avatars. It can be used to display the avatar of the currently logged in user or the avatar of other users.

## Example
<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserAvatar's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />
