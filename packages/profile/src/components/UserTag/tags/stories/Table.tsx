import { UserTagsContextValue } from "../UserTagsContext";
import UserTagsElements from "../UserTagsElements";

export const RenderProps = (props: Partial<UserTagsContextValue>) => {
  return null;
};
export const Subcomponents = (props: Partial<typeof UserTagsElements>) => {};
