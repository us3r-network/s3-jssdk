import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserTagsStories from "./UserTags.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/UserTag/tags/UserTags.module.css";

<Meta of={UserTagsStories} />

# UserTags

UserTags is a context component that has sub-components for displaying a user's tag information. It can display tags of either the currently logged-in user or other users.

## Example
<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserTags's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />

## Subcomponents

UserTags is a Context.Provider component that includes the following child components that can be used to render different parts of a form:

<ArgTypes of={Subcomponents} />
