import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserTagsStories from "./UserTags.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/UserTag/tags/UserTags.module.css";

<Meta of={UserTagsStories} />

# UserTags

UserTags 是一个 Context 组件，它有一些子组件，用于展示用户的标签信息，它可以是当前登录用户的标签，也可以是其他用户的标签。

```jsx
import { UserTags } from "@us3r-network/profile";
```

<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserTags 的 children 可以是一个函数，它接收一个对象作为参数，该对象包含以下属性：

<ArgTypes of={RenderProps} />

## Subcomponents

UserTags 是一个 Context.Provider 组件，它有一些子组件可以用来渲染表单的不同部分。

<ArgTypes of={Subcomponents} />
