import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as UserTagAddFormStories from "./UserTagAddForm.stories";
import { RenderProps, Subcomponents } from "./stories/Table";
import StyleCode from "!!raw-loader!../../../ui/UserTag/add-form/UserTagAddForm.module.css";

<Meta of={UserTagAddFormStories} />

# UserTagAddForm

UserTagAddForm is used to edit user tag information, and it is only available when the user is logged in.

## Example
<Canvas />

<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />

## Render Props

UserTagAddForm's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />

## Subcomponents

UserTagAddForm is a Context.Provider component that includes the following child components that can be used to render different parts of a form:

<ArgTypes of={Subcomponents} />
