import { Canvas, Meta, Controls, Source, ArgTypes } from "@storybook/blocks";

import * as LogoutButtonStories from "./LogoutButton.stories";
import { RenderProps } from "./stories/Table";
import StyleCode from "!!raw-loader!../../ui/LogoutButton/LogoutButton.module.css";

<Meta of={LogoutButtonStories} />

# LogoutButton

The LoginButton component internally triggers the sign out process of [@us3r-network/auth-with-rainbowkit](?path=/docs/auth-auth-with-rainbowkit--docs).

## Example
<Canvas />
<p>
  <details>
    <summary>View custom styles</summary>
    <Source code={StyleCode} />
  </details>
</p>

## Props

<Controls />



## Render Props

LogoutButton's children can be a function that receives an object as a parameter, which contains the following properties:

<ArgTypes of={RenderProps} />
